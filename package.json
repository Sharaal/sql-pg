{
  "name": "@sharaal/sql",
  "version": "1.0.0",
  "description": "Complex queries can be written with normal SQL, including the values needs to be bound and prefixed with the `sql` tag.",
  "repository": "sharaal/sql",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "lint": "standard",
    "lint:fix": "standard --fix"
  },
  "devDependencies": {
    "standard": "^12.0.1"
  }
}
