const assert = require('power-assert')

const sql = require('../')

describe('sql', () => {
  it('should extract and bind values', () => {})
  it('should escape keys for tables and columns', () => {})
  it('should extract and bind multiple values', () => {})
  it('should extract and bind list of multiple values', () => {})
  it('should support pairs of column keys and values using as set of updates', () => {})
  it('should support pairs of column keys and values using as set of conditions', () => {})
})
